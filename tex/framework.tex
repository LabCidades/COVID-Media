% To convert to PNG use
% convert -density 300 -transparent white -colorspace RGB output.pdf output.png

\documentclass[border=0.2cm]{standalone}
\usepackage{tikz}

\usetikzlibrary{shapes}
\usetikzlibrary{arrows}
\usetikzlibrary{positioning}
\usetikzlibrary{calc}
\usetikzlibrary{fit}

\tikzset{
	node distance=1.5cm,
	every node/.append style={align=center},
	every path/.append style={->},
	every matrix/.append style={draw, column sep=15, row sep=7},
	var/.style={draw,
		    ellipse,
	    	    minimum height=1.9cm,
	    	    minimum width=3.5cm}
}

\begin{document}

\begin{tikzpicture}
		    
	\node [var] (media) {media\\exposure};
	\node [var, right=of media] (risk) {risk\\perception};
	\node [var, right=of risk] (behavior) {adoption of\\protective\\behaviors};

	\path (media) edge node  {$+$ \\[0.5em]} (risk);
	\path (risk) edge node  {$+$ \\[0.5em]} (behavior);

	% control vars
	\node [matrix, below=2 of risk, anchor=center] (control) {
		\node{\textbf{Control Variables}}; \\
		\node [var] (selfeff) {Self-Efficacy}; &
		\node [var] (age) {Age}; &
		\node [var] (gender) {Gender}; \\
	};

\end{tikzpicture}

\end{document}
